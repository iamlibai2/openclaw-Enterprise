# 前端测试框架指南

## 概述

OpenClaw Control UI 前端使用 Vitest + @vue/test-utils 进行单元测试。

### 技术栈

| 工具 | 版本 | 用途 |
|------|------|------|
| Vitest | 4.x | 测试框架 |
| @vue/test-utils | 2.x | Vue 组件测试 |
| happy-dom | 20.x | DOM 环境 |
| @vitest/coverage-v8 | 4.x | 覆盖率统计 |

---

## 文件结构

```
frontend/
├── vitest.config.ts      # Vitest 配置
├── tests/
│   ├── setup.ts          # 测试环境配置
│   ├── validators.test.ts # 验证器测试
│   └── markdown.test.ts   # Markdown 工具测试
└── src/
    └── utils/            # 被测试的工具函数
```

---

## 运行测试

### 基本命令

```bash
# 进入前端目录
cd /home/iamlibai/.openclaw/workspace-aqiang/openclaw-control-ui/frontend

# 运行所有测试（监听模式）
npm test

# 运行所有测试（单次）
npm run test:run

# UI 模式
npm run test:ui

# 查看覆盖率
npm run test:coverage
```

---

## 编写测试

### 工具函数测试

```typescript
// tests/validators.test.ts
import { describe, it, expect } from 'vitest'
import { required, email } from '@/utils/validators'

describe('required', () => {
  it('应该对空值返回无效', () => {
    expect(required()('').valid).toBe(false)
  })

  it('应该对有效值返回有效', () => {
    expect(required()('test').valid).toBe(true)
  })
})
```

### Vue 组件测试

```typescript
// tests/components/MyComponent.test.ts
import { describe, it, expect } from 'vitest'
import { mount } from '@vue/test-utils'
import MyComponent from '@/components/MyComponent.vue'

describe('MyComponent', () => {
  it('应该正确渲染', () => {
    const wrapper = mount(MyComponent, {
      props: {
        title: 'Test Title'
      }
    })
    expect(wrapper.text()).toContain('Test Title')
  })
})
```

---

## 测试环境配置

### setup.ts

```typescript
// 全局存根 Element Plus 组件
config.global.stubs = {
  'el-button': true,
  'el-input': true,
  // ...
}

// Mock localStorage
Object.defineProperty(global, 'localStorage', {
  value: localStorageMock,
})
```

---

## 常用断言

```typescript
// 值相等
expect(value).toBe(expected)
expect(value).toEqual(expected)

// 布尔值
expect(value).toBeTruthy()
expect(value).toBeFalsy()

// 包含
expect(string).toContain('text')
expect(array).toContain(item)

// 长度
expect(array).toHaveLength(3)

// 抛出异常
expect(() => fn()).toThrow()

// 异步
await expect(promise).resolves.toBe(value)
await expect(promise).rejects.toThrow()
```

---

## 覆盖率

### 查看覆盖率

```bash
npm run test:coverage
```

### 输出

```
% Coverage report from v8
-------------------------|---------|----------|---------|---------|
File                     | % Stmts | % Branch | % Funcs | % Lines |
-------------------------|---------|----------|---------|---------|
All files                |   85.5  |   72.3   |   90.1  |   85.5  |
 validators.ts           |   95.2  |   88.1   |  100.0  |   95.2  |
 markdown.ts             |   78.3  |   65.4   |   83.3  |   78.3  |
-------------------------|---------|----------|---------|---------|
```

---

## 已知问题

### URL 校验器兼容性问题

`validators.ts` 中的 `url()` 函数使用 `url.netloc`，但 JavaScript URL API 没有此属性，应使用 `url.host`。

**临时方案：** 测试中跳过相关用例

---

## 测试状态

| 指标 | 数据 |
|------|------|
| 测试文件 | 2 个 |
| 测试用例 | 62 个 |
| 通过 | 61 个 |
| 跳过 | 1 个 |